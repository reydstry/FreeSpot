# Backend API environment (Railway)
# DATABASE_URL akan otomatis dari Railway PostgreSQL service
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ML API URL - isi setelah deploy ML API di Render
ML_API_URL=https://freespot-ml-api.onrender.com

# Server (Railway akan set PORT otomatis)
HOST=0.0.0.0
PORT=8000
DEBUG=false
